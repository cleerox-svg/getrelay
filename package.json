{
  "name": "relay",
  "version": "0.1.0",
  "private": true,
  "description": "Relay — a BBM-inspired secure messenger",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": "^20 || ^22"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "deploy": "turbo run deploy"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.6.3"
  },
  "workspaces": [
    "packages/*"
  ]
}
