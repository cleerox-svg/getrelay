# Copy to .env (gitignored) and adjust if needed.
# In dev: API at the worker dev server. In prod the Pages build sets this
# via the deploy environment variable.
VITE_API_BASE_URL=http://localhost:8787
