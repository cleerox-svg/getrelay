{
  "name": "@relay/ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo 'UI package is a stub until Session 4. See RELAY_BUILD_SPEC.md §11.' && exit 0",
    "build": "echo 'UI package is a stub until Session 4.' && exit 0",
    "typecheck": "echo 'UI package is a stub until Session 4.' && exit 0",
    "test": "echo 'UI package is a stub until Session 4.' && exit 0"
  }
}
