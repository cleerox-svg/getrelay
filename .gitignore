node_modules
.turbo
dist
build
.wrangler
.dev.vars
.dev.vars.*
!.dev.vars.example
.env
.env.*
!.env.example
.DS_Store
*.log
.vscode
.idea
coverage

# Capacitor — Android project is generated on demand by CI. If you ever
# need to customize it (signing, icons), run `pnpm cap add android` once
# locally and selectively commit the customized files.
packages/relay-ui/android
packages/relay-ui/ios
